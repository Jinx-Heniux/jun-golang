package main

/*
import (
	"fmt"

	"github.com/Jinx-Heniux/jun-golang/learn_concurrency"
)

func main() {
	c := make(chan int)
	quit := make(chan int)

	go func() {
		for i := 0; i < 6; i++ {
			fmt.Println(<-c)
		}
		quit <- 0
	}()

	learn_concurrency.Fibonacci(c, quit)
}
*/
